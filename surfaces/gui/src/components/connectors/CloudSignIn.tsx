// Cloud sign-in components — disabled. These are kept as no-op exports so
// existing imports don't break.

export function CloudSignInInline(_props: { blurb?: string }) {
  return null;
}

export function CloudStatusPending() {
  return null;
}
